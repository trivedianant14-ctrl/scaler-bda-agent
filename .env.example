# Anthropic (Claude)
ANTHROPIC_API_KEY=

# OpenAI (Whisper transcription)
OPENAI_API_KEY=

# Twilio WhatsApp
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# Default BDA number — pre-fills the "BDA WhatsApp Number" field in the dashboard.
# MUST have NEXT_PUBLIC_ prefix so Next.js exposes it to the browser bundle.
# The user can override it by typing in the field; this is just the prefill.
NEXT_PUBLIC_BDA_DEFAULT_NUMBER=
